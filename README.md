# Новый пианист — лендинг (прод)

**Живой сайт:** https://antioz.github.io/newpianist-landing/piano/
(GitHub Pages, ветка `main`. Пуш в `main` = деплой.)

Это **деплой-копия**. Источник правок и оригиналы фото живут в монорепо
`piano-school-tma` → папка `landing/` (на диске `/Users/imac/Documents/piano-school/landing/`).

Порядок изменения см. в `landing/piano/CLAUDE.md` источника (раздел «ГДЕ ЛЕНДИНГ
ЖИВЁТ И КАК ДЕПЛОИТСЯ»). Коротко: правишь там → переносишь контентные строки сюда →
копируешь `*-sq.jpg` ассеты → `git push` здесь.

Отличие `piano/index.html` от source — только трейлинг dev-панель tweaks (в source есть,
здесь стрипнута). Контент идентичен.
